# Build Projection

Prototype Streamlit app for Crestline Homes build projections.

## Run

```powershell
streamlit run app.py
```

The current prediction formula uses dummy coefficients for:

- Square footage
- Window square feet
- Cubic yards of concrete

Replace `dummy_prediction()` in `app.py` when real project data or a trained model is ready.
